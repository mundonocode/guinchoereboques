export default function ClientePage() {
    // O Mapa principal agora é renderizado no layout (ClientMapWrapper) 
    // para não ser desmontado (SPA navigation fluid) durante a troca de abas.
    return null;
}
