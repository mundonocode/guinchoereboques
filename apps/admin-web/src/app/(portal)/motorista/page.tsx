export default function MotoristaPage() {
    // O Mapa principal agora é renderizado no layout (MotoristaMapWrapper) 
    // para não ser desmontado (SPA navigation fluid) durante a troca de abas.
    return null;
}
